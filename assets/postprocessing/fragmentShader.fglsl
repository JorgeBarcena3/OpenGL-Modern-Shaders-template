#version 330
out vec4 frag_color;
  
in vec2 tx_coord;

uniform sampler2D screenTexture;

void main()
{ 
//    float media = tx_coord.x + tx_coord.y / 2;
    frag_color = texture(screenTexture, tx_coord);
}